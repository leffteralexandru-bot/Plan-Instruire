import { PROGRAM_AREA_THEMES } from '@/lib/programAreaTheme';

const retraining = PROGRAM_AREA_THEMES.retraining;

/** Fundal și borduri pentru containerele fluxului de re-instruire (distinct de instruirea inițială). */
export const RE_TRAINING_FLOW_SHELL = retraining.flowShell;
export const RE_TRAINING_FLOW_SHELL_SUCCESS = 'border-orange-300/80 bg-orange-50/35';
export const RE_TRAINING_FLOW_EYEBROW = retraining.flowEyebrow;
